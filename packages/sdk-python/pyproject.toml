[project]
name = "commons-sdk"
version = "1.0.0"
description = "Python SDK for the Commons Public API"
requires-python = ">=3.9"
dependencies = ["httpx>=0.24"]
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

/**
 * voter-client.ts — @voter-protocol/client removed (Cycle 17)
 *
 * The @voter-protocol/client SDK was dead code (DJB2 hash, zero callers,
 * no tests). Use @voter-protocol/noir-prover and @voter-protocol/crypto
 * directly for ZK proof generation and Poseidon2 hashing.
 *
 * On-chain submission: see district-gate-client.ts
 * Proof generation: see $lib/core/zkp/prover-client.ts
 */

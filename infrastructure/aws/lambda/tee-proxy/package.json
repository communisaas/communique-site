{
	"name": "tee-proxy",
	"version": "1.0.0",
	"description": "Lambda function to proxy requests to TEE instances",
	"main": "dist/index.js",
	"scripts": {
		"build": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"@aws-sdk/client-servicediscovery": "^3.0.0"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.0",
		"@types/node": "^18.0.0",
		"esbuild": "^0.17.0",
		"typescript": "^4.9.0"
	}
}
